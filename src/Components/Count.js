import React from 'react'

function Count({ text, earn}) {
    return (
        <div>
            hello new user you have completed {text} - and now have a total of {earn}
        </div>
    )
}

export default Count
